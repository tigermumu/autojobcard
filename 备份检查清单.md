# URL切换备份检查清单

在进行URL切换之前，请确认以下所有项目都已完成并验证。

## ✅ 备份前检查

### Git仓库状态
- [ ] Git仓库正常工作（`git status` 可以执行）
- [ ] 当前分支是 `master` 或主要开发分支
- [ ] 没有未提交的重要更改（或已决定如何处理）
- [ ] 远程仓库连接正常（如果有）

### 代码状态
- [ ] 所有代码文件已保存
- [ ] 没有正在运行的测试或构建
- [ ] 确认要备份的关键文件存在：
  - [ ] `backend/app/core/config.py`
  - [ ] `backend/app/services/workcard_import_service.py`
  - [ ] `backend/env.example`
  - [ ] `NRC R.py`
  - [ ] `内网请求URL清单.md`

### 磁盘空间
- [ ] 有足够的磁盘空间（至少需要项目大小的2-3倍）
- [ ] 备份目录路径可访问（`C:\AI\demo3-backup-vpn-version`）

---

## ✅ 备份执行检查

### Git标签备份
- [ ] 执行了 `git tag -a v1.0-vpn-version -m "VPN版本备份"`
- [ ] 标签创建成功（`git tag -l` 可以找到）
- [ ] 标签包含正确的提交（`git show v1.0-vpn-version` 可以查看）

### Git分支备份
- [ ] 执行了 `git branch backup/vpn-version`
- [ ] 分支创建成功（`git branch -a` 可以找到）
- [ ] 分支指向正确的提交

### 代码快照备份
- [ ] 备份目录已创建（`C:\AI\demo3-backup-vpn-version`）
- [ ] 关键文件已复制：
  - [ ] `backend/app/core/config.py` 存在
  - [ ] `backend/app/services/workcard_import_service.py` 存在
  - [ ] `backend/env.example` 存在
  - [ ] `NRC R.py` 存在
- [ ] 文件内容正确（可以打开查看）

---

## ✅ 备份验证

### 验证Git标签
```powershell
# 执行以下命令验证
git tag -l | Select-String "vpn-version"
git show v1.0-vpn-version --stat
```
- [ ] 标签存在
- [ ] 标签指向正确的提交
- [ ] 可以看到标签包含的文件列表

### 验证Git分支
```powershell
# 执行以下命令验证
git branch -a | Select-String "backup/vpn-version"
git log backup/vpn-version --oneline -5
```
- [ ] 分支存在
- [ ] 分支有提交历史
- [ ] 分支指向正确的提交

### 验证代码快照
```powershell
# 执行以下命令验证
Test-Path "C:\AI\demo3-backup-vpn-version"
Get-ChildItem "C:\AI\demo3-backup-vpn-version\backend\app\core\config.py"
Get-ChildItem "C:\AI\demo3-backup-vpn-version\backend\app\services\workcard_import_service.py"
```
- [ ] 快照目录存在
- [ ] 关键文件存在
- [ ] 文件内容完整

---

## ✅ 远程备份（如果有远程仓库）

### 推送标签
```powershell
git push origin v1.0-vpn-version
```
- [ ] 标签已推送到远程
- [ ] 远程仓库可以查看到标签

### 推送分支
```powershell
git push origin backup/vpn-version
```
- [ ] 分支已推送到远程
- [ ] 远程仓库可以查看到分支

---

## ✅ 恢复测试（可选但推荐）

### 测试从标签恢复
```powershell
# 创建测试分支
git checkout -b test-restore-from-tag v1.0-vpn-version

# 检查关键文件
Get-Content backend\app\core\config.py | Select-String "vpn.gameco.com.cn"

# 切换回原分支
git checkout master
git branch -D test-restore-from-tag
```
- [ ] 可以从标签恢复代码
- [ ] 恢复的代码包含VPN URL
- [ ] 可以正常切换回原分支

### 测试从分支恢复
```powershell
# 切换到备份分支
git checkout backup/vpn-version

# 检查关键文件
Get-Content backend\app\core\config.py | Select-String "vpn.gameco.com.cn"

# 切换回原分支
git checkout master
```
- [ ] 可以切换到备份分支
- [ ] 备份分支包含VPN URL
- [ ] 可以正常切换回原分支

---

## ✅ 备份记录

### 记录备份信息
- [ ] 备份时间：_________________
- [ ] 备份标签：`v1.0-vpn-version`
- [ ] 备份分支：`backup/vpn-version`
- [ ] 快照位置：`C:\AI\demo3-backup-vpn-version`
- [ ] 备份执行人：_________________
- [ ] 备份原因：URL从VPN版本切换到内网直连版本

### 记录关键文件哈希（可选）
```powershell
# 获取关键文件的哈希值
git hash-object backend\app\core\config.py
git hash-object backend\app\services\workcard_import_service.py
```
- [ ] config.py 哈希：_________________
- [ ] workcard_import_service.py 哈希：_________________

---

## ⚠️ 重要提醒

### 备份完成确认
- [ ] **所有备份都已成功创建**
- [ ] **所有备份都已验证**
- [ ] **已测试恢复功能（可选但推荐）**
- [ ] **已记录备份信息**

### 下一步操作
- [ ] 备份检查清单全部完成
- [ ] 可以开始进行URL切换
- [ ] 已准备好回滚方案

---

## 📝 备份完成确认

**我确认：**
- [ ] 已阅读并理解所有备份步骤
- [ ] 已完成所有必要的备份
- [ ] 已验证备份的有效性
- [ ] 已记录备份信息
- [ ] **准备开始URL切换操作**

**签名：** _________________  
**日期：** _________________

---

## 🔄 如果需要回滚

如果URL切换后出现问题，可以使用以下方法回滚：

### 方法1：从标签恢复
```powershell
git checkout -b restore-vpn-version v1.0-vpn-version
```

### 方法2：从分支恢复
```powershell
git checkout backup/vpn-version
```

### 方法3：从快照恢复
```powershell
robocopy C:\AI\demo3-backup-vpn-version C:\AI\demo3 /E /XD node_modules venv __pycache__ .git
```

---

**备份完成后，请在此清单上打勾确认，然后再进行URL切换！**

